import * as fs from 'fs';
import * as path from 'path';

import { green } from '../utilities';


interface InputEntry {
  min: number;
  max: number
}

export function solver(input: any[]) {
  return -1;
}